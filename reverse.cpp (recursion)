#include<iostream>
#include<string>
using namespace std;

int main()
{ 
void reverse(char*);
char string[]="mary";

reverse(string);
return 0;
}

void reverse(char *str)
  {
        if(*str)
             {
                reverse(str+1);
                 cout<<*str;
             }
  }
